LJMP START

P5 EQU 0F8H
P7 EQU 0DBH

ORG 100H

START:
	MOV DPTR, #8077H ; 1
	MOV A, #31
	MOVX @DPTR, A
	MOV DPTR, #807BH ; 2
	MOV A, #32
	MOVX @DPTR, A
	MOV DPTR, #807DH ; 3
	MOV A, #33
	MOVX @DPTR, A
	MOV DPTR, #807EH ; A
	MOV A, #41
	MOVX @DPTR, A

	MOV DPTR, #80B7H ; 4
	MOV A, #34
	MOVX @DPTR, A
	MOV DPTR, #80BBH ; 5
	MOV A, #35
	MOVX @DPTR, A
	MOV DPTR, #80BDH ; 6
	MOV A, #36
	MOVX @DPTR, A
	MOV DPTR, #80BEH ; B
	MOV A, #42
	MOVX @DPTR, A

	MOV DPTR, #80D7H ; 7
	MOV A, #37
	MOVX @DPTR, A
	MOV DPTR, #80DBH ; 8
	MOV A, #38
	MOVX @DPTR, A
	MOV DPTR, #80DDH ; 9
	MOV A, #39
	MOVX @DPTR, A
	MOV DPTR, #80DEH ; C
	MOV A, #43
	MOVX @DPTR, A

	MOV DPTR, #80E7H ; *
	MOV A, #2AH
	MOVX @DPTR, A
	MOV DPTR, #80EBH ; 0
	MOV A, #30
	MOVX @DPTR, A
	MOV DPTR, #80EDH ; #
	MOV A, #23
	MOVX @DPTR, A
	MOV DPTR, #80EEH ; D
	MOV A, #44
	MOVX @DPTR, A

MAINLOOP:
	MOV R0, #07FH
		MOV A, R0
		MOV P5, A
		MOV A, P7
		ANL A, R0
		MOV R2, A
		CLR C
		SUBB A, R0
		JZ NEXT1
		MOV A, R2
		MOV DPH, #80H
		MOV DPL, A
		MOVX A, @DPTR
		MOV P1, A
	NEXT1: MOV R0, #0BFH
		MOV A, R0
		MOV P5, A
		MOV A, P7
		ANL A, R0
		MOV R2, A
		CLR C
		SUBB A, R0
		JZ NEXT2
		MOV A, R2
		MOV DPH, #80H
		MOV DPL, A
		MOVX A, @DPTR
		MOV P1, A
	NEXT2: MOV R0, #0DFH
		MOV A, R0
		MOV P5, A
		MOV A, P7
		ANL A, R0
		MOV R2, A
		CLR C
		SUBB A, R0
		JZ NEXT3
		MOV A, R2
		MOV DPH, #80H
		MOV DPL, A
		MOVX A, @DPTR
		MOV P1, A
	NEXT3: MOV R0, #0EFH
		MOV A, R0
		MOV P5, A
		MOV A, P7
		ANL A, R0
		MOV R2, A
		CLR C
		SUBB A, R0
		JZ MAINLOOP
		MOV A, R2
		MOV DPH, #80H
		MOV DPL, A
		MOVX A, @DPTR
		MOV P1, A
JMP MAINLOOP

NOP
NOP
NOP
END START
